% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cqt.R
\name{plot_cqt_gof}
\alias{plot_cqt_gof}
\title{Goodness-of-fit plot for concentration-QTc model}
\usage{
plot_cqt_gof(
  ermod,
  n_bins = 10,
  qi_width_obs = 0.9,
  qi_width_sim = 0.9,
  threshold = 10,
  exposure_to_mark = NULL,
  show_obs_points = FALSE,
  show_caption = TRUE,
  num_exposures = 51,
  n_draws_sim = NULL,
  seed_sample_draws = NULL
)
}
\arguments{
\item{ermod}{An object of class \code{ermod_cqt}, from \code{\link[=dev_ermod_cqt]{dev_ermod_cqt()}}.}

\item{n_bins}{Number of concentration bins (quantiles) for the observed
data summary. Default is 10 (deciles).}

\item{qi_width_obs}{Width of the confidence interval for the mean of
observed \eqn{\Delta\Delta QTc} in each bin. Default is 0.9.}

\item{qi_width_sim}{Width of the credible interval for the model
prediction. Default is 0.9.}

\item{threshold}{Value to draw a horizontal reference line. Default is 10
(ms). Set to \code{NULL} to omit.}

\item{exposure_to_mark}{Named or unnamed vector of concentrations (e.g.
geometric mean Cmax at therapeutic and supratherapeutic doses) at which
the model-predicted \eqn{\Delta\Delta QTc} is highlighted with point
ranges. Default is \code{NULL} (not shown).}

\item{show_obs_points}{Logical, whether to show individual observed
\eqn{\Delta\Delta QTc} values. Default is \code{FALSE}.}

\item{show_caption}{Logical, whether to show the caption note for the plot.
Default is \code{TRUE}.}

\item{num_exposures}{Number of concentration values to simulate for the
model prediction line.}

\item{n_draws_sim}{Number of draws for simulation. If NULL (default),
all draws in the model object are used.}

\item{seed_sample_draws}{Seed for sampling draws. Default is NULL.}
}
\value{
A ggplot object
}
\description{
Plot the model-predicted placebo-adjusted change from baseline QTc
(\eqn{\Delta\Delta QTc}) against concentration, overlaid with the observed
\eqn{\Delta\Delta QTc} summarized in concentration bins (e.g. deciles).
The observed \eqn{\Delta\Delta QTc} is calculated for active treatment
by subtracting the mean \eqn{\Delta QTc} of placebo at the matching
nominal time (if \code{var_time} was specified in \code{\link[=dev_ermod_cqt]{dev_ermod_cqt()}}; otherwise
the overall placebo mean). If there is no placebo data, the observed
\eqn{\Delta QTc} is shown instead.
}
\examples{
\donttest{
data(d_sim_cqt)

ermod_cqt <- dev_ermod_cqt(
  data = d_sim_cqt,
  var_resp = "DQTCF",
  var_exposure = "CONC_1000",
  var_random = "ID",
  var_trt = "TRT",
  var_time = "NTIME",
  var_baseline = "QTCFBL",
  # Settings to make the example run faster
  chains = 2,
  iter = 1000
)

ermod_cqt

# Predicted placebo-adjusted change from baseline QTc at
# geometric mean Cmax of therapeutic and supratherapeutic doses
sim_cqt_ddqtc(ermod_cqt, c("200 mg" = 0.95, "800 mg" = 4.52))

plot_cqt_gof(ermod_cqt, exposure_to_mark = c("200 mg" = 0.95, "800 mg" = 4.52))
}

}
