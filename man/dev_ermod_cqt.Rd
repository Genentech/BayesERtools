% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cqt.R
\name{dev_ermod_cqt}
\alias{dev_ermod_cqt}
\title{Develop concentration-QTc (C-QT) model}
\usage{
dev_ermod_cqt(
  data,
  var_resp,
  var_exposure,
  var_random,
  var_trt = NULL,
  trt_placebo = NULL,
  var_time = NULL,
  var_baseline = NULL,
  var_cov = NULL,
  random_effect_type = c("both", "intercept", "slope"),
  prior = rstanarm::default_prior_coef(stats::gaussian()),
  prior_intercept = rstanarm::default_prior_intercept(stats::gaussian()),
  prior_aux = rstanarm::exponential(autoscale = TRUE),
  prior_covariance = rstanarm::decov(),
  adapt_delta = NULL,
  verbosity_level = 1,
  chains = 4,
  iter = 2000
)
}
\arguments{
\item{data}{Input data for E-R analysis}

\item{var_resp}{Name of the response variable, typically the change from
baseline in QTc (\eqn{\Delta QTc}), in character.}

\item{var_exposure}{Name of the drug concentration variable in character.
The concentration should be 0 for placebo.}

\item{var_random}{Name of the grouping variable for the random effects
(typically subject ID) in character.}

\item{var_trt}{Name of the treatment variable in character. If \code{NULL}
(default), the treatment-specific intercept is not included and the
placebo-adjusted observed \eqn{\Delta\Delta QTc} can not be calculated
(i.e. no placebo data).}

\item{trt_placebo}{Value of \code{var_trt} that indicates placebo. All other
values are considered as active treatment. If \code{NULL} (default), \code{0} is used
for numeric or logical (\code{FALSE}) \code{var_trt}, and \code{"Placebo"} for character
or factor \code{var_trt}.}

\item{var_time}{Name of the nominal time variable in character. If
specified, it is included in the model as a categorical variable
(time-specific intercept) and used for time-matched placebo adjustment of
the observed data.}

\item{var_baseline}{Name of the baseline QTc variable in character. If
specified, it is included in the model after centering by its mean.}

\item{var_cov}{Names of additional covariates in character vector.}

\item{random_effect_type}{Type of random effects to include for each level
of \code{var_random}:
\itemize{
\item \code{"both"} (default): correlated random intercept and random slope on the
exposure, i.e. \code{(1 + exposure | ID)}
\item \code{"intercept"}: random intercept only, i.e. \code{(1 | ID)}
\item \code{"slope"}: random slope on the exposure only, i.e. \code{(0 + exposure | ID)}
}}

\item{prior, prior_intercept, prior_aux}{See \code{\link[rstanarm:stan_glm]{rstanarm::stan_glm()}}}

\item{prior_covariance}{Prior for the covariance matrices of the random
effects. See \code{\link[rstanarm:priors]{rstanarm::decov()}} and \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}}.}

\item{adapt_delta}{Target average acceptance probability for the NUTS
sampler. If \code{NULL} (default), the default of \code{\link[rstanarm:stan_glmer]{rstanarm::stan_glmer()}}
(0.95) is used. Increase it (e.g. to 0.99) if there are warnings about
divergent transitions. See \link[rstanarm:adapt_delta]{rstanarm::adapt_delta} for details.}

\item{verbosity_level}{Verbosity level. 0: No output, 1: Display steps,
2: Display progress in each step, 3: Display MCMC sampling.}

\item{chains}{Number of chains for Stan.}

\item{iter}{Number of iterations for Stan.}
}
\value{
An object of class \code{ermod_cqt}, which is a subclass of \code{ermod_lme}.
}
\description{
This function develops a linear mixed-effects model for
concentration-QTc (C-QT) analysis, following the structure of the
pre-specified model recommended in the scientific white paper by
Garnett et al. (2018). It is a wrapper of \code{\link[=dev_ermod_lme]{dev_ermod_lme()}}, which uses
\code{\link[rstanarm:stan_glmer]{rstanarm::stan_lmer()}} (precompiled Stan models) for model fitting.
}
\details{
The model is:

\deqn{\Delta QTc_{ij} = (\theta_0 + \eta_{0,i}) + \theta_1 TRT_{i} +
(\theta_2 + \eta_{2,i}) C_{ij} + \theta_{3,j} TIME_{j} +
\theta_4 (QTc_{i,baseline} - \overline{QTc_{baseline}}) + \epsilon_{ij}}

where \eqn{TRT} is the indicator for active treatment (treatment-specific
intercept), \eqn{C} is the drug concentration, \eqn{TIME} is the nominal
time as a categorical variable, and \eqn{QTc_{baseline}} is the baseline
QTc. The random effects on the intercept and slope are specified with
\code{random_effect_type}. Each of the treatment, time, and baseline terms is
only included if the corresponding variable is specified.

The following derived columns are added to the data and used in the model:
\itemize{
\item \verb{<var_trt>_active}: 1 for active treatment and 0 for placebo
\item \verb{<var_time>_fct}: \code{var_time} as a factor
\item \verb{<var_baseline>_cent}: \code{var_baseline} centered by its mean
}

If you supply \code{newdata} for simulation functions such as \code{\link[=sim_er]{sim_er()}}, it
needs to contain these derived columns. \code{\link[=sim_cqt_ddqtc]{sim_cqt_ddqtc()}} and
\code{\link[=plot_cqt_gof]{plot_cqt_gof()}} take care of them internally.
}
\examples{
\donttest{
data(d_sim_cqt)

ermod_cqt <- dev_ermod_cqt(
  data = d_sim_cqt,
  var_resp = "DQTCF",
  var_exposure = "CONC_1000",
  var_random = "ID",
  var_trt = "TRT",
  var_time = "NTIME",
  var_baseline = "QTCFBL",
  # Settings to make the example run faster
  chains = 2,
  iter = 1000
)

ermod_cqt

# Predicted placebo-adjusted change from baseline QTc at
# geometric mean Cmax of therapeutic and supratherapeutic doses
sim_cqt_ddqtc(ermod_cqt, c("200 mg" = 0.95, "800 mg" = 4.52))

plot_cqt_gof(ermod_cqt, exposure_to_mark = c("200 mg" = 0.95, "800 mg" = 4.52))
}

}
\references{
Garnett C, et al. Scientific white paper on concentration-QTc modeling.
J Pharmacokinet Pharmacodyn. 2018;45(3):383-397.
\doi{10.1007/s10928-017-9558-5}
}
\seealso{
\code{\link[=sim_cqt_ddqtc]{sim_cqt_ddqtc()}}, \code{\link[=plot_cqt_gof]{plot_cqt_gof()}}, \code{\link[=plot_cqt_hysteresis]{plot_cqt_hysteresis()}}
}
