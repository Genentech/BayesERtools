% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cqt.R
\name{sim_cqt_ddqtc}
\alias{sim_cqt_ddqtc}
\title{Simulate placebo-adjusted change from baseline QTc}
\usage{
sim_cqt_ddqtc(
  ermod,
  exposure_to_sim_vec,
  threshold = 10,
  qi_width = 0.9,
  output_type = c("median_qi", "draws"),
  n_draws_sim = NULL,
  seed_sample_draws = NULL
)
}
\arguments{
\item{ermod}{An object of class \code{ermod_cqt}, from \code{\link[=dev_ermod_cqt]{dev_ermod_cqt()}}.}

\item{exposure_to_sim_vec}{Vector of concentrations to simulate. If named,
the names are returned in the \code{.label} column.}

\item{threshold}{Threshold of \eqn{\Delta\Delta QTc} (in the unit of the
response) for calculating the posterior probability of exceeding it.
Default is 10 (ms), following ICH E14.}

\item{qi_width}{Width of the credible interval. Default is 0.9, i.e. the
upper bound corresponds to the one-sided 95\% upper bound used in ICH E14.}

\item{output_type}{Type of output. "median_qi" (default) returns the
median and credible interval, and "draws" returns the posterior draws.}

\item{n_draws_sim}{Number of draws for simulation. If NULL (default),
all draws in the model object are used.}

\item{seed_sample_draws}{Seed for sampling draws. Default is NULL.}
}
\value{
A tibble. With \code{output_type = "median_qi"}, it has one row per
concentration with the following columns:
\itemize{
\item \code{.label}: names of \code{exposure_to_sim_vec} (only if named)
\item \verb{<var_exposure>}: concentration
\item \code{.ddqtc}, \code{.lower}, \code{.upper}: median and credible interval of
\eqn{\Delta\Delta QTc}
\item \code{.width}: width of the credible interval
\item \code{.prob_exceed}: posterior probability of \eqn{\Delta\Delta QTc}
exceeding \code{threshold}
}

With \code{output_type = "draws"}, it has one row per concentration and draw,
with \code{.draw} and \code{.ddqtc} columns.
}
\description{
Calculate the model-predicted placebo-adjusted change from baseline QTc
(\eqn{\Delta\Delta QTc}) at specified concentrations, e.g. the geometric
mean Cmax at therapeutic and supratherapeutic doses. The prediction is at
the population level (random effects set to zero), and is defined as the
difference in the predicted \eqn{\Delta QTc} between active treatment at
the specified concentration and placebo (concentration of 0), at the same
time point and baseline QTc. In the model from \code{\link[=dev_ermod_cqt]{dev_ermod_cqt()}}, it
equals \eqn{\theta_1 + \theta_2 C}.
}
\examples{
\donttest{
data(d_sim_cqt)

ermod_cqt <- dev_ermod_cqt(
  data = d_sim_cqt,
  var_resp = "DQTCF",
  var_exposure = "CONC_1000",
  var_random = "ID",
  var_trt = "TRT",
  var_time = "NTIME",
  var_baseline = "QTCFBL",
  # Settings to make the example run faster
  chains = 2,
  iter = 1000
)

ermod_cqt

# Predicted placebo-adjusted change from baseline QTc at
# geometric mean Cmax of therapeutic and supratherapeutic doses
sim_cqt_ddqtc(ermod_cqt, c("200 mg" = 0.95, "800 mg" = 4.52))

plot_cqt_gof(ermod_cqt, exposure_to_mark = c("200 mg" = 0.95, "800 mg" = 4.52))
}

}
